<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Classic Charm Co.</title>
    <style>
        body {
            background-color: rgb(225, 188, 218);
        }
      </style>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>Information</h2>          
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Product Name</th>
                    <th>Product Image</th>
                    <th>Quantity</th>
                    <th>Price</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < accessory.length; i++) { %>
                    <tr>
                        <td><a href="/addaccessories/<%= accessory[i].idaccessory %>"></a><%= accessory[i].name %></td>
                        <td><img src = "images/<%= accessory[i].image %>" width="20%"></td>
                        <td><%= accessory[i].quantity %></td>
                        <td>$<%= accessory[i].price.toFixed(2) %></td>
                        <td><a href="/editaccessories/<%= accessory[i].idaccessory %>">Edit</a></td>
                        <td><a href="/deleteaccessories/<%= accessory[i].idaccessory %>" onclick="return confirm('Are you sure you want to delete this accessory?')">Delete</a></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>